---
title: List Domains
api: GET /domains
description: "List all domains"
---

# List Domains

Retrieve a list of all domains in your account with their verification status.

## Request

<CodeGroup>
```bash cURL
curl "https://app.uselettr.com/api/domains" \
  -H "Authorization: Bearer le_xxxxxxxxxxxx"
```

```javascript Node.js
const domains = await lettr.domains.list();
```

```php PHP
$domains = $lettr->domains->list();
```

```python Python
domains = lettr.domains.list()
```
</CodeGroup>

## Response

```json
{
  "results": [
    {
      "domain": "secure.example.com",
      "cname_status": "valid",
      "compliance_status": "valid",
      "dkim_status": "valid",
      "created_at": "2024-01-15T10:30:00Z",
      "updated_at": "2024-01-15T10:35:00Z"
    },
    {
      "domain": "mail.example.com",
      "cname_status": "valid",
      "compliance_status": "valid",
      "dkim_status": "valid",
      "created_at": "2024-01-14T08:00:00Z",
      "updated_at": "2024-01-14T08:05:00Z"
    },
    {
      "domain": "notifications.example.com",
      "cname_status": "pending",
      "compliance_status": "pending",
      "dkim_status": "pending",
      "created_at": "2024-01-16T12:00:00Z",
      "updated_at": null
    }
  ]
}
```

## Response Fields

### Domain Object

| Field | Type | Description |
|-------|------|-------------|
| `domain` | string | The domain name |
| `cname_status` | string | CNAME record verification status |
| `compliance_status` | string | Compliance verification status |
| `dkim_status` | string | DKIM record verification status |
| `created_at` | string \| null | ISO 8601 timestamp when the domain was created |
| `updated_at` | string \| null | ISO 8601 timestamp when the domain was last updated |

## Status Values

| Status | Description |
|--------|-------------|
| `valid` | Record is properly configured and verified |
| `pending` | Verification is in progress |
| `invalid` | Record is missing or misconfigured |
| `unverified` | Record has not been verified yet |
