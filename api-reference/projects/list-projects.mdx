---
title: List Projects
openapi: GET /projects
---

Retrieve a paginated list of projects for your team. Each project includes its name, optional emoji icon, and timestamps.

Projects organize your email templates into logical groups. Use the `per_page` and `page` query parameters to control pagination.
