---
title: Update a topic
openapi: PATCH /audience/topics/{topicId}
---

Partially update a topic. The `default_subscription` field is immutable. Requires the `audience:write` scope and is blocked for sandbox API keys.
