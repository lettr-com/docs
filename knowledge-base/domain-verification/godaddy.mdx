---
title: GoDaddy
description: "Verify your domain on GoDaddy with Lettr"
---

# GoDaddy

This guide walks you through adding DNS records on GoDaddy to verify your domain with Lettr.

<Tip>
  It is best practice to use a subdomain (e.g., `sender.example.com`) instead of the root domain. Using a subdomain allows for proper reputation segmentation and is especially important if receiving emails with Lettr.
</Tip>

## Add Domain to Lettr

Log in to your [Lettr Dashboard](https://app.lettr.com), go to **Domains**, open the tab for your domain type (e.g. **Sending Domains**), and click **+ New Sending Domain** (or the equivalent for Tracking, Inbound, or Storage). Enter your domain name.

## Log in to GoDaddy

1. Log in to your [GoDaddy account](https://sso.godaddy.com)
2. Navigate to **My Products** → **Domains**
3. Click **DNS** next to your domain
4. This will open the DNS management page

---

## Sending Domain Setup

Choose one of the following options based on whether you're using a subdomain or primary domain.

### Option 1: Subdomain (CNAME Verification)

If you're setting up a subdomain like `sender.example.com`, add the following records:

#### Bounce Domain CNAME

1. Click **Add New Record**
2. Set the **Type** to `CNAME`
3. Enter `sender` for the **Name** (or your chosen subdomain)
4. Enter `sparkpostmail.com` for the **Value**
5. Set **TTL** to `600` (or use default)
6. Click **Save**

| GoDaddy | Lettr | Example Value |
|---------|-------|---------------|
| Type | Type | `CNAME` |
| Name | Host | `sender` |
| Value | Value | `sparkpostmail.com` |
| TTL | - | `600` |

### Option 2: Primary Domain (TXT SPF Verification)

If you're using your root domain `example.com`, add an SPF record:

#### SPF Record

1. Click **Add New Record**
2. Set the **Type** to `TXT`
3. Enter `@` for the **Name** (root domain)
4. Enter `v=spf1 include:_spf.sparkpostmail.com ~all` for the **Value**
5. Set **TTL** to `600`
6. Click **Save**

| GoDaddy | Lettr | Example Value |
|---------|-------|---------------|
| Type | Type | `TXT` |
| Name | Host | `@` |
| Value | Value | `v=spf1 include:_spf.sparkpostmail.com ~all` |
| TTL | - | `600` |

<Info>
  If you already have an SPF record, merge Lettr's include with your existing record.
</Info>

### DKIM Record (Required for All Sending Domains)

1. Click **Add New Record**
2. Set the **Type** to `TXT`
3. Copy the **Name** from your Lettr dashboard (e.g., `scph0226._domainkey.sender`)
4. Copy the **Value** from your Lettr dashboard
5. Set **TTL** to `600`
6. Click **Save**

| GoDaddy | Lettr | Example Value |
|---------|-------|---------------|
| Type | Type | `TXT` |
| Name | Host | `scph0226._domainkey.sender` |
| Value | Value | `v=DKIM1;k=rsa;h=sha256;p=MIGfMA0...` |
| TTL | - | `600` |

<Info>
  The DKIM hostname and value are unique to your domain. Copy them exactly from your Lettr dashboard.
</Info>

### DMARC Record (Required for All Sending Domains)

1. Click **Add New Record**
2. Set the **Type** to `TXT`
3. Enter `_dmarc.sender` for the **Name**
4. Enter `v=DMARC1;p=none;` for the **Value**
5. Set **TTL** to `600`
6. Click **Save**

| GoDaddy | Lettr | Example Value |
|---------|-------|---------------|
| Type | Type | `TXT` |
| Name | Host | `_dmarc.sender` |
| Value | Value | `v=DMARC1;p=none;` |
| TTL | - | `600` |

---

## Tracking Domain Setup

To set up a custom tracking domain, add two CNAME records:

### SSL Certificate CNAME

1. Click **Add New Record**
2. Set the **Type** to `CNAME`
3. Copy the **Name** from your Lettr dashboard (e.g., `_acme-challenge.click.sender`)
4. Copy the **Value** from your Lettr dashboard
5. Click **Save**

| GoDaddy | Lettr | Example Value |
|---------|-------|---------------|
| Type | Type | `CNAME` |
| Name | Host | `_acme-challenge.click.sender` |
| Value | Value | (provided in dashboard) |

### Traffic CNAME

1. Click **Add New Record**
2. Set the **Type** to `CNAME`
3. Enter your tracking subdomain for the **Name** (e.g., `click.sender`)
4. Enter `proxy.lettr-tracking.com` for the **Value**
5. Click **Save**

| GoDaddy | Lettr | Example Value |
|---------|-------|---------------|
| Type | Type | `CNAME` |
| Name | Host | `click.sender` |
| Value | Value | `proxy.lettr-tracking.com` |

<Tip>
  SSL certificates are automatically generated and renewed by Lettr. All tracking URLs use HTTPS.
</Tip>

---

## Inbound Domain Setup

To receive emails at your domain, add MX records:

1. Click **Add New Record**
2. Set the **Type** to `MX`
3. Enter `inbound` for the **Name**
4. Enter `rx1.sparkpostmail.com` for the **Value**
5. Enter `10` for the **Priority**
6. Click **Save**

Repeat for the other two MX records (`rx2.sparkpostmail.com` and `rx3.sparkpostmail.com`):

| GoDaddy | Lettr | Example Value |
|---------|-------|---------------|
| Type | Type | `MX` |
| Name | Host | `inbound` |
| Value | Value | `rx1.sparkpostmail.com` |
| Priority | Priority | `10` |

| GoDaddy | Lettr | Example Value |
|---------|-------|---------------|
| Type | Type | `MX` |
| Name | Host | `inbound` |
| Value | Value | `rx2.sparkpostmail.com` |
| Priority | Priority | `10` |

| GoDaddy | Lettr | Example Value |
|---------|-------|---------------|
| Type | Type | `MX` |
| Name | Host | `inbound` |
| Value | Value | `rx3.sparkpostmail.com` |
| Priority | Priority | `10` |

<Warning>
  When you configure MX records, all emails sent to that domain/subdomain will be routed to Lettr. We recommend using a subdomain like `inbound.example.com`.
</Warning>

---

## Complete Verification

Return to your Lettr dashboard and click **Verify**. DNS propagation typically takes a few minutes to a few hours.

## Troubleshooting

<AccordionGroup>
  <Accordion title="Records not detected">
    GoDaddy may take up to 48 hours to propagate DNS changes. Use [WhatsMyDNS](https://www.whatsmydns.net/) to check propagation status.
  </Accordion>
  <Accordion title="CNAME conflict with existing records">
    A CNAME record cannot coexist with other record types on the same hostname. Remove conflicting records first.
  </Accordion>
  <Accordion title="SPF record not working">
    Ensure you only have one SPF record. Multiple SPF records will cause verification to fail.
  </Accordion>
</AccordionGroup>
